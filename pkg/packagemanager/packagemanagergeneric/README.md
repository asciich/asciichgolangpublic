# packagemanagergeneric

Generic package manager with automatic OS detection.

## Overview

The `packagemanagergeneric` package provides a unified interface for package management across different Linux distributions. It automatically detects the operating system and selects the appropriate package manager (apt-get, pacman, or yay).
