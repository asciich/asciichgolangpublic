# filescmd specifications

This are the specifications for the [`filescmd` package](README.md).

This document extends the parent specifications [defaultclicommands.spec.md](../defaultclicommands.spec.md).

## Implementation

- The `files` command must at least implement these subcommands.
    - `mkdir` to ensure a directory exists.
