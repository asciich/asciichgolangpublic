# objectscmd specifications

## Implementations

- The `objects` command must at least implement these sub commands:
    - `delete` to delete an object.
    - `list` to list all objects in the given `--bucket`.
    - `upload-file` to upload a local file into the `--bucket`.
- Add a usage example in the long description for every command.
