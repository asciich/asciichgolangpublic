# s3cmd - userscmd specifications

## Implementation

- The `users` command must implement the subcommands:
    - `create` to create a user.
    - `delete` to delete a user.
    - `list` to list all users.
